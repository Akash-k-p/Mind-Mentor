import GuidedMeditation from './GuidedMeditation';

// Assuming you're using a method to initialize your app here
const root = document.getElementById('root'); // Ensure you have a div with id 'root' in your HTML
if (root) {
    GuidedMeditation(); // Call your GuidedMeditation function or render it here
}
